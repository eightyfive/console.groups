{
  "name": "console.groups",
  "version": "0.1.1",
  "scripts": {
    "test": "jest",
    "release": "np --no-2fa --no-draft-release"
  },
  "dependencies": {
    "console.unicorn": "^0.1.1"
  },
  "devDependencies": {
    "jest": "^25.3.0",
    "prettier": "^2.0.4"
  }
}
